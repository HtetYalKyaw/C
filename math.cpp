#include<iostream>
#include<cmath>
using namespace std;

int main(){

    double x = 4.245;
    double y = 3;
    double z;

    //z = max(x , y); max = chose the maximum
    //z = min(x , y); min = chose the mininum
    //z = pow(2, 3); pow = 2 power 3
    //z = sqrt(3); sqrt = square root of 
    //z = abs (-24); abs = absolute value 
    //z = round(x);
    z = ceil(x);
    

    cout << z;

    return 0;
}